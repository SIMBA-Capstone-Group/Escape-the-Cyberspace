using UnityEngine;
using TMPro;

public class AtbashPuzzle : MonoBehaviour
{
    public TMP_Text messageBox;
    
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
